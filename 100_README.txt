- 0.5 beta 3 (2015-12-24):

- 0.5 beta 4 (2016-01-07):
	+ Re construct code:
		+ AngularJS now uses templateURL inside routeProvider.
		+ IncomeTarget and IncomeAdjust still use the same controller and template.
		+ SalesTarget and SalesAdjust use different controller and template.
	+ New features:
		+ CopyData from Sales_adjustment_salesman and Sales_adjustment_intercom to Sales_report_adjustment.
		+ Transfer Sales_report_adjustment from dev to product.
		+ Upload GL_adjustment with version.
		+ Transfer GL_adjustment.
	+ Update EXCEL template files.